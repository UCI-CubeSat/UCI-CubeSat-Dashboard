const DEFAULT_ZOOM = 8;
const DEFAULT_LATITUDE = 33.6405;
const DEFAULT_LONGITUDE = -117.8443;
const DEFAULT_MAP_SETTING = {
  dragPan: true,
  dragRotate: false,
  scrollZoom: true,
  keyboard: true,
  doubleClickZoom: true,
  minZoom: 0,
  maxZoom: 20,
  minPitch: 0,
  maxPitch: 85
};

export {DEFAULT_ZOOM, DEFAULT_LONGITUDE, DEFAULT_LATITUDE, DEFAULT_MAP_SETTING};
